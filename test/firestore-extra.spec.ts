import { DocRef } from '../src/firestore-extra'

test('example', () => {
    let _doc: DocRef<{a:1}> = {} as any;
    expect('Tested in other projects e2e tests. No standalone regression tests for now, so use it at your own risk.').toBe('Tests to be created.');
});